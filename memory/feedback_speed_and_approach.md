---
name: Speed and working style feedback
description: User wants fast, efficient execution — minimal back-and-forth, just build
type: feedback
---

User values fast execution and minimal interruptions.

**Why:** They explicitly said "keep the speed good" after a session where we moved quickly through navbar and footer ports without asking for confirmation at each step.

**How to apply:** Default to building immediately when the intent is clear. Batch related work (e.g. component + CSS Module + layout wiring) in one go. Don't ask for approval on obvious decisions like file placement or CSS class naming. Keep responses short. Don't re-read files after editing. Read only what's needed. Avoid redundant tool calls.
